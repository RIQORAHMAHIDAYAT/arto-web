# URL REST API ARTO backend (tanpa trailing slash).
# Contoh untuk dev lokal:
VITE_API_URL=http://localhost:3000/api

# Kredensial akun demo untuk tombol "Isi otomatis" di halaman login.
# HANYA tampil di mode development (import.meta.env.DEV) dan jika keduanya terisi.
# Nilai harus cocok dengan akun seed di arto-backend (prisma/seed.ts).
VITE_DEMO_EMAIL=demo@arto.id
VITE_DEMO_PASSWORD=demopass123
